bottom_row_w_title <- plot_grid(
ggdraw() + draw_label("Distributions in learned songs", fontfamily = "Avenir Next", fontface = "bold", size = 14, x = 0, hjust = 0), bottom_row,
ncol = 1, rel_heights = c(0.1, 1)
)
column_titles <- plot_grid(
ggdraw() + draw_label("       Zipf's Law of Abbrev.", fontfamily = "Avenir Next", fontface = "bold", color = "#0072B2"),
ggdraw() + draw_label("       Menzerath's Law", fontfamily = "Avenir Next", fontface = "bold", color = "#D55E00"),
ggdraw() + draw_label("       Zipf's Rank-Freq. Law", fontfamily = "Avenir Next", fontface = "bold", color = "#009E73"),
nrow = 1
)
#combine elements
main_plot <- plot_grid(top_row_w_title, bottom_row_w_title, ncol = 1)
final_plot <- plot_grid(main_plot, column_titles, ncol = 1, rel_heights = c(1, 0.04))
#get finch image
finch <- get_phylopic(uuid = "7bf8ca33-0fef-4985-a4d4-a147bff6e98c")
#save plot
png("plots/distributions.png", width = 8, height = 5.4, units = "in", res = 1200)
plot(1)
final_plot
add_phylopic_base(finch, fill = "#0072B2", height = 0.08, x = 1.44, y = 1.585)
dev.off()
fixef(combined_model)
#load libraries
library(brms)
library(readxl)
library(ape)
library(ggtree)
library(ggplot2)
library(dplyr)
library(hues)
library(cowplot)
library(rphylopic)
#set working directory
setwd("~/Documents/Work/Fall 2025/Primate ZLA")
#load in data
data <- read_xlsx("data.xlsx")
tree <- read.nexus("10ktrees_consensus_tree.nex")[[2]]
#note: sapajus flavius had frequencies as percents rather than counts, I have converted to counts based on sample size in the paper
data <- data.frame(species = gsub(" ", "_", data$Species), duration = data$`Mean duration (ms)`, count = data$Frequency)
# #explore mismatched species
# unique_species <- unique(data$species)
# missing_species <- c(1:length(unique_species))[-which(unique_species %in% tree$tip.label)]
# missing_species <- unique_species[missing_species]
#overwrite mismatched species names (some are the same, some close, some close enough)
tree$tip.label[which(tree$tip.label == "Eulemur_macaco_macaco")] <- "Eulemur_macaco" #same
tree$tip.label[which(tree$tip.label == "Galago_senegalensis")] <- "Galagos_senegalensis" #same
tree$tip.label[which(tree$tip.label == "Gorilla_gorilla_gorilla")] <- "Gorilla_gorilla" #same
tree$tip.label[which(tree$tip.label == "Macaca_cyclopis")] <- "Macaca_cyclopsis" #same
tree$tip.label[which(tree$tip.label == "Macaca_fascicularis")] <- "Macaca_fasicularis" #same
tree$tip.label[which(tree$tip.label == "Pithecia_irrorata")] <- "Pithecia_chrysocephala" #close
tree$tip.label[which(tree$tip.label == "Presbytis_melalophos")] <- "Presbytis_johnii" #close
tree$tip.label[which(tree$tip.label == "Semnopithecus_entellus")] <- "Presbytis_entellus" #same
tree$tip.label[which(tree$tip.label == "Cebus_albifrons")] <- "Sapajus_flavius" #close(ish)
tree$tip.label[which(tree$tip.label == "Cebus_olivaceus")] <- "Sapajus_libidinosus" #close(ish)
#drop all other tips from tree
tree <- drop.tip(tree, setdiff(tree$tip.label, data$species))
#ensure matching tip labels
data$species <- factor(data$species, levels = tree$tip.label)
#get covariance matrix from tree
tree_covar <- vcv.phylo(tree, corr = TRUE)
#iterate through species and apply log transformation and group mean centering
data_scaled <- do.call(rbind, lapply(unique(data$species), function(x){
temp <- data[which(data$species == x),]
temp$duration <- scale(log(temp$duration))
temp$count <- scale(log(temp$count))
return(temp)
}))
#run combined model
#because of group mean centering above, species is only a random slope and not an intercept
#capture output is used to prevent sampling from overwhelming the markdown file
output <- capture.output(
combined_model <- brm(
duration ~ count + (0 + count|gr(species, cov = tree_covar)),
data = data_scaled,
family = gaussian(),
data2 = list(tree_covar = tree_covar),
prior = c(
prior(normal(0, 0.5), class = "b"),
prior(normal(0, 0.5), class = "Intercept"),
prior(exponential(2), class = "sd"),
prior(exponential(2), class = "sigma")
),
chains = 10,
cores = 10,
iter = 10000,
backend = "cmdstanr"
)
)
#overall coefficients
fixef(combined_model)
#calculation of phylogenetic signal: https://cran.r-project.org/web/packages/brms/vignettes/brms_phylogenetics.html
phylo_signal <- hypothesis(combined_model, class = NULL, "sd_species__count^2/(sd_species__count^2 + sigma^2) = 0")
phylo_signal <- phylo_signal$hypothesis
phylo_signal
#to extract the coefficients for each species
species_coefs <- coef(combined_model)$species[, , 1]
species_coefs
fixef(combined_model)
fixef(combined_model)[2, ]
fixef(combined_model)[2, -2]
effects <- fixef(combined_model)[2, -2]
paste0(effects[1])
paste0(round(effects[1], 4))
paste0(round(effects[1], 4), ", 95%: ", round(effects[2], 4), "---", round(effects[3], 4))
phylo_signal
phylo_signal[2]
#iterate through species and apply log transformation and group mean centering
data_scaled <- do.call(rbind, lapply(unique(data$species), function(x){
temp <- data[which(data$species == x),]
temp$duration <- scale(log(temp$duration))
temp$count <- scale(log(temp$count))
return(temp)
}))
#run combined model
#because of group mean centering above, species is only a random slope and not an intercept
#capture output is used to prevent sampling from overwhelming the markdown file
output <- capture.output(
combined_model <- brm(
duration ~ count + (0 + count|gr(species, cov = tree_covar)),
data = data_scaled,
family = gaussian(),
data2 = list(tree_covar = tree_covar),
prior = c(
prior(normal(0, 0.5), class = "b"),
prior(normal(0, 0.5), class = "Intercept"),
prior(exponential(2), class = "sd"),
prior(exponential(2), class = "sigma")
),
chains = 10,
cores = 10,
iter = 10000,
backend = "cmdstanr"
)
)
save(combined_model, file = "model.RData")
#iterate through species and apply log transformation and group mean centering
data_scaled <- do.call(rbind, lapply(unique(data$species), function(x){
temp <- data[which(data$species == x),]
temp$duration <- scale(log(temp$duration))
temp$count <- scale(log(temp$count))
return(temp)
}))
#run combined model
#because of group mean centering above, species is only a random slope and not an intercept
#capture output is used to prevent sampling from overwhelming the markdown file
set.seed(12345)
output <- capture.output(
combined_model <- brm(
duration ~ count + (0 + count|gr(species, cov = tree_covar)),
data = data_scaled,
family = gaussian(),
data2 = list(tree_covar = tree_covar),
prior = c(
prior(normal(0, 0.5), class = "b"),
prior(normal(0, 0.5), class = "Intercept"),
prior(exponential(2), class = "sd"),
prior(exponential(2), class = "sigma")
),
chains = 10,
cores = 10,
iter = 10000,
backend = "cmdstanr"
)
)
save(combined_model, file = "model.RData")
#load libraries
library(brms)
library(rstan)
library(readxl)
library(ape)
library(ggtree)
library(ggplot2)
library(dplyr)
library(hues)
library(cowplot)
library(rphylopic)
#set working directory
setwd("~/Documents/Work/Fall 2025/Primate ZLA")
#load in data
data <- read_xlsx("data.xlsx")
tree <- read.nexus("tree.nex")[[2]]
#note: sapajus flavius had frequencies as percents rather than counts, I have converted to counts based on sample size in the paper
data <- data.frame(species = gsub(" ", "_", data$Species), duration = data$`Mean duration (ms)`, count = data$Frequency)
# #explore mismatched species
# unique_species <- unique(data$species)
# missing_species <- c(1:length(unique_species))[-which(unique_species %in% tree$tip.label)]
# missing_species <- unique_species[missing_species]
#overwrite mismatched species names (some are the same, some close, some close enough)
tree$tip.label[which(tree$tip.label == "Eulemur_macaco_macaco")] <- "Eulemur_macaco" #same
tree$tip.label[which(tree$tip.label == "Galago_senegalensis")] <- "Galagos_senegalensis" #same
tree$tip.label[which(tree$tip.label == "Gorilla_gorilla_gorilla")] <- "Gorilla_gorilla" #same
tree$tip.label[which(tree$tip.label == "Macaca_cyclopis")] <- "Macaca_cyclopsis" #same
tree$tip.label[which(tree$tip.label == "Macaca_fascicularis")] <- "Macaca_fasicularis" #same
tree$tip.label[which(tree$tip.label == "Pithecia_irrorata")] <- "Pithecia_chrysocephala" #close
tree$tip.label[which(tree$tip.label == "Presbytis_melalophos")] <- "Presbytis_johnii" #close
tree$tip.label[which(tree$tip.label == "Semnopithecus_entellus")] <- "Presbytis_entellus" #same
tree$tip.label[which(tree$tip.label == "Cebus_albifrons")] <- "Sapajus_flavius" #close(ish)
tree$tip.label[which(tree$tip.label == "Cebus_olivaceus")] <- "Sapajus_libidinosus" #close(ish)
#drop all other tips from tree
tree <- drop.tip(tree, setdiff(tree$tip.label, data$species))
#ensure matching tip labels
data$species <- factor(data$species, levels = tree$tip.label)
#get covariance matrix from tree
tree_covar <- vcv.phylo(tree, corr = TRUE)
data
x <- unique(data$species)[1]
temp <- data[which(data$species == x),]
temp
temp$duration <- temp$duration*temp$count
temp
data_summed <- do.call(rbind, lapply(unique(data$species), function(x){
temp <- data[which(data$species == x),]
temp$duration <- temp$duration*temp$count
temp$duration <- scale(log(temp$duration))
temp$count <- scale(log(temp$count))
return(temp)
}))
data_summed <- do.call(rbind, lapply(unique(data$species), function(x){
temp <- data[which(data$species == x),]
temp$duration <- temp$duration*temp$count
temp$duration <- scale(log(temp$duration))
temp$count <- scale(log(temp$count))
return(temp)
}))
#run combined model
#because of group mean centering above, species is only a random slope and not an intercept
#capture output is used to prevent sampling from overwhelming the markdown file
set.seed(12345)
output <- capture.output(
combined_model_summed <- brm(
duration ~ count + (0 + count|gr(species, cov = tree_covar)),
data = data_summed,
family = gaussian(),
data2 = list(tree_covar = tree_covar),
prior = c(
prior(normal(0, 0.5), class = "b"),
prior(normal(0, 0.5), class = "Intercept"),
prior(exponential(2), class = "sd"),
prior(exponential(2), class = "sigma")
),
chains = 10,
cores = 10,
iter = 10000,
backend = "cmdstanr"
)
)
combined_model_summed
#to extract the coefficients for each species
species_coefs <- coef(combined_model_summed)$species[, , 1]
species_coefs
#iterate through species, convert mean duration to cumulative duration, and apply log transformation and group mean centering
data_summed <- do.call(rbind, lapply(unique(data$species), function(x){
temp <- data[which(data$species == x),]
temp$duration <- temp$duration*temp$count
temp$duration <- scale(log(temp$duration))
temp$count <- scale(log(temp$count))
return(temp)
}))
#run combined model on cumulative durations
#if effects are present, then sole (2010) explanation is not a problem
#https://arxiv.org/pdf/1207.3169
#https://onlinelibrary.wiley.com/doi/epdf/10.1002/cplx.20326
#because of group mean centering above, species is only a random slope and not an intercept
#capture output is used to prevent sampling from overwhelming the markdown file
set.seed(12345)
output <- capture.output(
combined_model_summed <- brm(
duration ~ count + (0 + count|gr(species, cov = tree_covar)),
data = data_summed,
family = gaussian(),
data2 = list(tree_covar = tree_covar),
prior = c(
prior(normal(0, 0.5), class = "b"),
prior(normal(0, 0.5), class = "Intercept"),
prior(exponential(2), class = "sd"),
prior(exponential(2), class = "sigma")
),
chains = 10,
cores = 10,
iter = 10000,
backend = "cmdstanr"
)
)
#iterate through species, convert mean duration to cumulative duration, and apply log transformation and group mean centering
data_summed <- do.call(rbind, lapply(unique(data$species), function(x){
temp <- data[which(data$species == x),]
temp$duration <- temp$duration*temp$count
temp$duration <- scale(log(temp$duration))
temp$count <- scale(log(temp$count))
return(temp)
}))
#run combined model on cumulative durations
#if effects are present, then sole (2010) explanation is not a problem
#https://arxiv.org/pdf/1207.3169
#https://onlinelibrary.wiley.com/doi/epdf/10.1002/cplx.20326
#because of group mean centering above, species is only a random slope and not an intercept
#capture output is used to prevent sampling from overwhelming the markdown file
set.seed(12345)
output <- capture.output(
combined_model_summed <- brm(
duration ~ count + (0 + count|gr(species, cov = tree_covar)),
data = data_summed,
family = gaussian(),
data2 = list(tree_covar = tree_covar),
prior = c(
prior(normal(0, 0.5), class = "b"),
prior(normal(0, 0.5), class = "Intercept"),
prior(exponential(2), class = "sd"),
prior(exponential(2), class = "sigma")
),
chains = 10,
cores = 10,
iter = 10000,
backend = "cmdstanr"
)
)
save(combined_model_summed, file = "model_summed.RData")
#set working directory
setwd("/Users/masonyoungblood/Documents/Work/Spring_2025/Critica Analysis/infodemiology_interventions")
#load models
load("effects/after_effects.RData")
after_effects''
after_effects
load("effects/match_effects.RData")
match_effects
after_effects
load("effects/intervenction_effects_subset.RData")
load("effects/intervenction_effects.RData")
load("effects/intervention_effects.RData")
load("effects/intervention_effects_subset.RData")
load("effects/intervention_effects.RData")
load("effects/intervention_effects.RData")
load("effects/intervention_effects_subset.RData")
load("effects/intervention_subset_effects.RData")
intervention_effects
intervention_subset_effects
load("models/intervention_subset_models.RData")
load("/Users/masonyoungblood/Documents/Work/Spring_2025/Critica Analysis/infodemiology_interventions/models/intervention_subset_model.RData")
intervention_subset_model
#load libraries
library(brms)
library(rstan)
intervention_subset_model
read.csv("~/Downloads/beja/phoneme_boundaries.csv")
data <- read.csv("~/Downloads/beja/phoneme_boundaries.csv")
data <- read.csv("~/Downloads/beja/raw_phoneme_boundaries.csv")
table(data$tx)
table(data$ph)
sort(table(data$ph))
data <- read.csv("~/Downloads/beja/phoneme_boundaries.csv")
hist(data$offset - data$onset)
hist(data$offset - data$onset, breaks = 100)
quantile(data$offset - data$onset, probs = c(0.05, 0.95))
install.packages("colorspace")
colorspace::::as_LAB("#6BA368")
colorspace::as_LAB("#6BA368")
colorspace::hex2RGB("#6BA368")
?colorspace::LAB
library(colorspace)
as(hex2RGB("#6BA368"), "LAB")
LAB(as(hex2RGB("#6BA368"), "LAB"))
as(hex2RGB("#6BA368"), "LAB")
as(hex2RGB("#6BA368"), "LAB")
as(hex2RGB("DD99BB"), "LAB")
as(hex2RGB("#DD99BB"), "LAB")
dist(as(hex2RGB("#6BA368"), "LAB"), as(hex2RGB("#DD99BB"), "LAB"))
dist(rbind(as(hex2RGB("#6BA368"), "LAB"), as(hex2RGB("#DD99BB")), "LAB"))
dist(rbind(as(hex2RGB("#6BA368"), "LAB"), as(hex2RGB("#DD99BB"), "LAB")))
rbind(as(hex2RGB("#6BA368"), "LAB"), as(hex2RGB("#DD99BB"), "LAB"))
as(hex2RGB("#6BA368"), "LAB")
dist(rbind(c(as(hex2RGB("#6BA368"), "LAB")), c(as(hex2RGB("#DD99BB"), "LAB"))))
c(as(hex2RGB("#6BA368"), "LAB"))
dist(rbind(as.numeric(as(hex2RGB("#6BA368"), "LAB")), as.numeric(as(hex2RGB("#DD99BB"), "LAB"))))
dist(rbind(c(as(hex2RGB("#6BA368"), "LAB"))[[1]], c(as(hex2RGB("#DD99BB"), "LAB")))[[1]])
c(as(hex2RGB("#6BA368"), "LAB"))[[1]]
?dist
str(as(hex2RGB("#6BA368"), "LAB"))
rbind(
c(as(hex2RGB("#6BA368"), "LAB")),
c(as(hex2RGB("#DD99BB"), "LAB"))
)[1,]
rbind(
c(as(hex2RGB("#6BA368"), "LAB")),
c(as(hex2RGB("#DD99BB"), "LAB"))
)
rbind(
(as(hex2RGB("#6BA368"), "LAB")),
(as(hex2RGB("#DD99BB"), "LAB"))
)
rbind(
c(as(hex2RGB("#6BA368"), "LAB")),
c(as(hex2RGB("#DD99BB"), "LAB"))
)
rbind(
as.matrix(as(hex2RGB("#6BA368"), "LAB")),
as.matrix(as(hex2RGB("#DD99BB"), "LAB"))
)
as.matrix(as(hex2RGB("#6BA368"), "LAB"))
rbind(
unlist(as(hex2RGB("#6BA368"), "LAB")),
unlist(as(hex2RGB("#DD99BB"), "LAB"))
)
unlist(as(hex2RGB("#6BA368"), "LAB"))
rbind(
(as(hex2RGB("#6BA368"), "LAB"))[1, ],
(as(hex2RGB("#DD99BB"), "LAB"))[1, ]
)
rbind(
as(hex2RGB("#6BA368"), "LAB")[1, ],
as(hex2RGB("#DD99BB"), "LAB")[1, ]
)
as(hex2RGB("#6BA368"), "LAB")[1, ]
c(as(hex2RGB("#6BA368"), "LAB"), as(hex2RGB("#DD99BB"), "LAB"))
unlist(c(as(hex2RGB("#6BA368"), "LAB"), as(hex2RGB("#DD99BB"), "LAB")))
unlist(c(as(hex2RGB("#6BA368"), "LAB")[[1]], as(hex2RGB("#DD99BB"), "LAB")))
hai <- as(hex2RGB("#6BA368"), "LAB")
str(hai)
hai@coords
rbind(
as(hex2RGB("#6BA368"), "LAB")@coords,
as(hex2RGB("#DD99BB"), "LAB")@coords
)
dist(
rbind(
as(hex2RGB("#6BA368"), "LAB")@coords,
as(hex2RGB("#DD99BB"), "LAB")@coords
)
)
HSL
colorspace::HLS(c(60, 100, 50))
colorspace::HSL(c(60, 100, 50))
colorspace::HLS(c(60, 100, 50))
?HLS
color_b <- c(70, 90, 10)
HLS(color_a[1], color_a[2], color_a[3)
HLS(color_a[1], color_a[2], color_a[3])
color_a <- c(60, 100, 50)
color_b <- c(70, 90, 10)
HLS(color_a[1], color_a[2], color_a[3])
as(HLS(color_a[1], color_a[2], color_a[3]), "LAB")
as(HLS(color_a[1], color_a[2], color_a[3]), "LAB")
as(HLS(color_a[1], color_a[2], color_a[3]), "sRGB")
as(as(HLS(color_a[1], color_a[2], color_a[3]), "sRGB"), "LAB")
as(hex2RGB("#6BA368"), "LAB")
colordist <- function(hex_a, hex_b){
dist(
rbind(
as(hex2RGB(hex_a), "LAB")@coords,
as(hex2RGB(hex_b), "LAB")@coords
)
)
}
colordist("#6BA368", "#DD99BB")
colordist <- function(hex_a, hex_b){
c(dist(
rbind(
as(hex2RGB(hex_a), "LAB")@coords,
as(hex2RGB(hex_b), "LAB")@coords
)
))
}
colordist("#6BA368", "#DD99BB")
colordist <- function(hex_a, hex_b){
c(
dist(
rbind(
colorspace::as(colorspace::hex2RGB(hex_a), "LAB")@coords,
colorspace::as(colorspace::hex2RGB(hex_b), "LAB")@coords
)
)
)
}
colordist("#6BA368", "#DD99BB")
colordist <- function(hex_a, hex_b){
c(
dist(
rbind(
as(hex2RGB(hex_a), "LAB")@coords,
as(hex2RGB(hex_b), "LAB")@coords
)
)
)
}
load("/Users/masonyoungblood/Documents/Work/Fall 2025/ce_art_biblio/data/works_proc_final.RData")
works_proc$references
unique(unlist(works_proc$references))
length(unique(unlist(works_proc$references)))
#set working directory
setwd("~/Documents/Work/Fall 2025/ce_art_biblio")
#load libraries
library(jsonlite)
library(openalexR)
library(igraph)
library(bib2df)
library(data.table)
library(ggraph)
library(ggiraph)
library(htmlwidgets)
library(parallel)
library(stringdist)
library(patchwork)
library(quanteda)
library(tidylo)
library(dplyr)
library(cowplot)
library(ggrepel)
#load processed works object
load("data/works_proc_final.RData")
which(works_proc$year < 2013)
length(which(works_proc$year < 2023)) - length(which(works_proc$year < 2013))
(length(which(works_proc$year < 2023)) - length(which(works_proc$year < 2013)))/length(which(works_proc$year < 2013))
((length(which(works_proc$year < 2023)) - length(which(works_proc$year < 2013)))/length(which(works_proc$year < 2013)))*100
